architecture rtl of e is
  signal x, y, z : bit;
  constant K : integer := 1 +;
  constant J : integer := (1 + 2;
begin
  x <= y +;
  y <= x and;
  z <= (x;
  x <= y @ z;
  process
  begin
    if x = then
      y <= '1';
    end if;
    while loop
      null;
    end loop;
    case is
      when others => null;
    end case;
    wait for;
    z <= x;
  end process;
  y <= x;
end architecture rtl;
