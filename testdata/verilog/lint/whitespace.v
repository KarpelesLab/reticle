// Whitespace the formatter will fix later.
// lint-config: off:all warn:line-length warn:trailing-whitespace warn:tabs
// max-line-length: 60
module ws (input wire a, output wire y);   
	assign y = a;
    // this comment is deliberately longer than sixty characters wide
endmodule
